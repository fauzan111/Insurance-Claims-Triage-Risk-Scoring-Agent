# Dev, test, and eval tooling. Installed in CI's test stage.
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
ruff==0.8.4

# LLM-output eval (faithfulness, relevancy, classification accuracy).
# Pulls its own OpenTelemetry stack — kept out of runtime deps to avoid pin conflicts.
deepeval==2.1.5
